name: Monitor Pull Requests
on:
  pull_request:
    # Ditambahkan synchronize agar memantau setiap ada commit push baru masuk ke PR
    types: [opened, closed, reopened, synchronize]

permissions:
  actions: write
  contents: read
  pull-requests: read

jobs:
  kirim-notifikasi-pr:
    runs-on: ubuntu-latest # Membutuhkan ubuntu-latest untuk kalkulasi git diff internal
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0 # Ambil seluruh riwayat commit untuk menghitung statistik perubahan

      - name: Hitung Statistik Perubahan Kode PR
        id: pr_stats
        run: |
          # Dapatkan data statistik perubahan langsung dari REST API GitHub Event Context
          COMMITS_COUNT="${{ github.event.pull_request.commits }}"
          CHANGED_FILES="${{ github.event.pull_request.changed_files }}"
          
          # Hitung selisih baris kode menggunakan git diff native terhadap base branch
          git fetch origin ${{ github.event.pull_request.base.ref }}
          DIFF_STATS=$(git diff --shortstat origin/${{ github.event.pull_request.base.ref }} HEAD || echo "0 files changed, 0 insertions(+), 0 deletions(-)")
          
          # Ekstrak data penambahan dan pengurangan baris kode
          ADDITIONS=$(echo "$DIFF_STATS" | grep -oE '[0-9]+ insertion' | awk '{print $1}' || echo "0")
          DELETIONS=$(echo "$DIFF_STATS" | grep -oE '[0-9]+ deletion' | awk '{print $1}' || echo "0")
          
          [ -z "$ADDITIONS" ] && ADDITIONS=0
          [ -z "$DELETIONS" ] && DELETIONS=0

          echo "TOTAL_COMMITS=$COMMITS_COUNT" >> $GITHUB_OUTPUT
          echo "CHANGED_FILES=$CHANGED_FILES" >> $GITHUB_OUTPUT
          echo "ADDITIONS=$ADDITIONS" >> $GITHUB_OUTPUT
          echo "DELETIONS=$DELETIONS" >> $GITHUB_OUTPUT

      - name: Atur Payload Gaya Bisnis Ningguang
        id: format
        run: |
          ACTION="${{ github.event.action }}"
          echo "COLOR=16239121" >> $GITHUB_OUTPUT
          echo "FOOTER=💎 'Setiap transaksi harus mendatangkan nilai keuntungan bagi Liyue.'" >> $GITHUB_OUTPUT
          
          INDEX=$((RANDOM % 3))
          
          if [ "$ACTION" = "opened" ] || [ "$ACTION" = "synchronize" ]; then
            if [ "$ACTION" = "synchronize" ]; then
              echo "TITLE=🔄 Pembaruan Dokumen Kontrak: Amandemen Baru Masuk" >> $GITHUB_OUTPUT
              echo "DESC=Mitra bisnis kita baru saja mengirimkan lembar amandemen kodingan baru ke dalam proposal ini. Jade Chamber akan langsung memperbarui kalkulasi aset dan memeriksa perubahan statistiknya." >> $GITHUB_OUTPUT
            elif [ $INDEX -eq 0 ]; then
              echo "TITLE=📜 Proposal Kontrak Kerja Baru Tiba" >> $GITHUB_OUTPUT
              echo "DESC=Sebuah draf penggabungan kode baru telah diajukan ke Jade Chamber. Jika proposal investasi baris kode ini menguntungkan stabilitas proyek, saya akan segera menandatanganinya." >> $GITHUB_OUTPUT
            elif [ $INDEX -eq 1 ]; then
              echo "TITLE=📈 Analisis Pasar: Peluang Investasi Kode" >> $GITHUB_OUTPUT
              echo "DESC=Sekretarisku baru saja membawakan dokumen pengajuan kode ini. Mari kita bedah bersama, apakah kontribusi baru ini akan menaikkan nilai kapitalisasi repositori kita atau tidak." >> $GITHUB_OUTPUT
            else
              echo "TITLE=🔍 Pemeriksaan Ketat Draf Perjanjian" >> $GITHUB_OUTPUT
              echo "DESC=Jangan biarkan ada celah hukum atau bug sekecil apa pun lolos dari pengawasan Jade Chamber. Kita akan melakukan audit menyeluruh terhadap pengajuan baru ini." >> $GITHUB_OUTPUT
            fi
            
          elif [ "$ACTION" = "closed" ]; then
            IS_MERGED="${{ github.event.pull_request.merged }}"
            if [ "$IS_MERGED" = "true" ]; then
              echo "TITLE=🤝 Kontrak Disetujui dan Disahkan" >> $GITHUB_OUTPUT
              echo "DESC=Kesepakatan telah dicapai! Draf proposal ini resmi digabungkan ke dalam cabang utama. Kerja sama yang sangat menguntungkan." >> $GITHUB_OUTPUT
            else
              echo "TITLE=❌ Proposal Kontrak Ditolak" >> $GITHUB_OUTPUT
              echo "DESC=Setelah pertimbangan yang matang di Jade Chamber, investasi kode ini dinilai tidak membawa keuntungan bagi kestabilan proyek. Berkas dibatalkan." >> $GITHUB_OUTPUT
            fi
          else
            echo "TITLE=📊 Pengajuan Ulang Proposal Bisnis" >> $GITHUB_OUTPUT
            echo "DESC=Draf proposal penggabungan kode yang sempat tertunda kini telah diajukan kembali untuk dilakukan kalkulasi ulang biaya dan risikonya." >> $GITHUB_OUTPUT
          fi

      - name: Coba Ambil ID Pesan Discord Lama Berdasarkan Nomor PR
        id: load_msg_id
        continue-on-error: true
        uses: actions/cache/restore@v4
        with:
          path: discord_msg_id.txt
          key: discord-pr-msg-${{ github.event.pull_request.number }}

      - name: Baca Teks ID Pesan
        id: msg_check
        run: |
          if [ -f "discord_msg_id.txt" ]; then
            MSG_ID=$(cat discord_msg_id.txt)
            echo "EXISTS=true" >> $GITHUB_OUTPUT
            echo "MSG_ID=$MSG_ID" >> $GITHUB_OUTPUT
          else
            echo "EXISTS=false" >> $GITHUB_OUTPUT
          fi

      - name: Susun JSON Struktur Embed Melalui JQ
        id: build_json
        run: |
          REPO_NAME="${{ github.repository }}"
          REPO_URL="${{ github.server_url }}/$REPO_NAME"
          BRANCH_NAME="${{ github.event.pull_request.head.ref }}"
          DOC_TITLE="${{ github.event.pull_request.title }}"
          DOC_NUM="#${{ github.event.pull_request.number }}"
          DOC_URL="${{ github.event.pull_request.html_url }}"
          USER_ACTOR="${{ github.actor }}"

          # Format baris statistik ala Github Web
          STAT_COMMITS="🔢 ${{ steps.pr_stats.outputs.TOTAL_COMMITS }} commits"
          STAT_FILES="📂 ${{ steps.pr_stats.outputs.CHANGED_FILES }} files changed"
          STAT_LINES="🟩 +${{ steps.pr_stats.outputs.ADDITIONS }}  🟥 -${{ steps.pr_stats.outputs.DELETIONS }} lines"

          jq -n \
            --arg title "${{ steps.format.outputs.TITLE }}" \
            --arg desc "${{ steps.format.outputs.DESC }}" \
            --arg color "${{ steps.format.outputs.COLOR }}" \
            --arg repo_name "$REPO_NAME" \
            --arg repo_url "$REPO_URL" \
            --arg doc_title "$DOC_TITLE" \
            --arg doc_num "$DOC_NUM" \
            --arg doc_url "$DOC_URL" \
            --arg actor "$USER_ACTOR" \
            --arg s_commits "$STAT_COMMITS" \
            --arg s_files "$STAT_FILES" \
            --arg s_lines "$STAT_LINES" \
            --arg branch "$BRANCH_NAME" \
            --arg footer_text "${{ steps.format.outputs.FOOTER }}" \
            '{
              embeds: [{
                title: $title,
                description: $desc,
                color: ($color | tonumber),
                fields: [
                  { name: "🏰 Lokasi Pemeriksaan", value: ("[" + $repo_name + " (" + $branch + ")](" + $repo_url + ")"), inline: false },
                  { name: "💼 Judul Perjanjian", value: ("[" + $doc_title + " " + $doc_num + "](" + $doc_url + ")"), inline: false },
                  { name: "📊 Audit Stat Kode (GitHub Live)", value: ($s_commits + "\n" + $s_files + "\n" + $s_lines), inline: false },
                  { name: "👤 Pemohon Kontrak", value: ("`" + $actor + "`"), inline: true }
                ],
                footer: { text: $footer_text }
              }]
            }' > pr_payload.json

      - name: Eksekusi Kirim / Edit Pesan Discord via Bot API
        run: |
          CHANNEL_ID="${{ secrets.DISCORD_PR_CHANNEL_ID }}"
          BOT_TOKEN="${{ secrets.DISCORD_PR_TOKEN }}"
          
          if [ "${{ steps.msg_check.outputs.EXISTS }}" = "true" ]; then
            # EDIT PESAN LAMA (Jika ini adalah event commit baru/synchronize)
            echo "Mengedit pesan Discord lama dengan ID: ${{ steps.msg_check.outputs.MSG_ID }}"
            RESPONSE=$(curl -s -X PATCH \
              -H "Authorization: Bot $BOT_TOKEN" \
              -H "Content-Type: application/json" \
              -d @pr_payload.json \
              "https://discord.com{{ steps.msg_check.outputs.MSG_ID }}")
          else
            # KIRM PESAN BARU PERTAMA KALI (Jika PR baru dibuka)
            echo "Mengirim notifikasi kontrak baru pertama kali..."
            RESPONSE=$(curl -s -X POST \
              -H "Authorization: Bot $BOT_TOKEN" \
              -H "Content-Type: application/json" \
              -d @pr_payload.json \
              "https://discord.com")
            
            # Ekstrak ID pesan baru yang sukses dikirim dan simpan untuk amandemen berikutnya
            NEW_MSG_ID=$(echo "$RESPONSE" | jq -r '.id')
            if [ "$NEW_MSG_ID" != "null" ]; then
              echo "$NEW_MSG_ID" > discord_msg_id.txt
            fi
          fi

      - name: Simpan Kunci ID Pesan ke Sistem Cache Monitor
        if: steps.msg_check.outputs.EXISTS == 'false'
        uses: actions/cache/save@v4
        with:
          path: discord_msg_id.txt
          key: discord-pr-msg-${{ github.event.pull_request.number }}
