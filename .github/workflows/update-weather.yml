name: Update Weather
on:
  workflow_dispatch:
  schedule:
  - cron: '0 1 * * *'

jobs:
  update-weather:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.x'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install requests

      - name: Run update script to refresh weather available
        run: python weather.py

      - name: Commit and Push changes
        uses: EndBug/add-and-commit@v9
        with:
          author_name: TheKingTermux
          author_email: 50316075+TheKingTermux@users.noreply.github.com
          message: "Otomatis: Update cuaca di README.md"
          add: 'README.md status.json' 
